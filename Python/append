from operator import truediv


def llenar(x):
    a=int(input("Escribe un numero"))
    arr.append(a)
    x+=1
    if x < 10:
        llenar(x)
        
def mostrar():
    for x in range(0,len(arr)):
        print(x,"\n")
    
def pila():
    for x in range(len(arr,0)) -1:
        arr.pop(x)
        mostrar()
def liberarArreglo():
    valor=int(input("Escribe la posicion a eliminar"))
    if valor>=0 and valor<len(arr):
        arr.pop (valor)
    else:
        print("Posicion incorrecta")
x=0
arr = []
llenar(x)
mostrar()
pila()




'''p=False
while(True):
    liberarArreglo()
    mostrar() 
    res = input("Deseas otro ejecucion")
    if res == "si" or res=="s":
        p
    else:
        break'''