from curses.ascii import isalpha, isdigit


def validarletras(a):
    x=0
    for i in a:
      
         if isalpha(i):
    
        
    
b = input(("Escribe un nombre"))
validarletras(a)